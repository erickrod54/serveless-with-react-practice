import React from "react";

/**serveless-with-react-practice app version 4 - Product
 * Component - Features:
 * 
 *      --> Building a basic 'Product' Component
 *         to test the  routing   
 * 
 * Note: After setting it up the routing the message of 
 * this Component should be displayed once i click on the 
 * 'Airtable' > 'Product' picture   
 */

const Product = () => {

    return(
        <h2>single product page</h2>
    )
}

export default Product;